# GROK BOT 화이트 랜딩 QA

final result: passed

최종 사용자 방향: 흰 배경, Pretendard Bold/Thin, 최소 문구, Black/White/유광 Silver, 실제 문서 아래로 내려가는 4개 제품 소개.

## 실화면 검증
- 데스크톱 1280×800: 디스플레이 섹션 top=0에서 노브 top=800. 휠 스크롤 후 디스플레이 top=-800, 노브 top=0. 고정 문구 교체가 아닌 문서 스크롤임을 확인.
- 4개 섹션 순서와 좌우를 바꾸는 모델을 실제 화면에서 확인. 마지막 분해 장면은 하판까지 표시.
- 모바일 390×844: 문구와 제품의 상하 배치, 모델 이동, 내부 구조 분해, 가로 넘침 없음.
- computed style: main 배경 rgb(255,255,255), 제목 Pretendard Grok 700, em 100.
- Silver 선택 시 헤더/상품 옵션 연동, 본체/키캡의 금속 반사와 하이라이트 확인. White는 전체 키캡과 본체를 밝게 변경.
- 가격 199,000원과 출시 알림 창 확인. 이메일 누락, 동의 누락, 저장, 삭제 동작을 확인하고 검증용 qa@example.com 정보는 UI에서 삭제.
- 콘솔 오류 없음. 주 스크롤 모델은 화면 밖에서 렌더링 중단. 원래 페이지와 root 설정은 보존.

## 한계
- 실제 이메일 접수·발송 서버는 미연결. 화면은 기기 내 저장과 그 상태를 정확히 안내한다.
- 내부 구조는 레퍼런스에 기반한 콘셉트 모델이다. 제조용 사양으로 표현하지 않는다.
- 실제 휴대폰 하드웨어 대신 390×844 브라우저에서 검증.

## 기록
- `docs/design-references/grok-landing-white/`에 검증 화면 저장.
- 폰트 원본: https://github.com/orioncactus/pretendard (v1.3.9, SIL OFL).

## 후속 요청 검증 — BSD 로고, 직접 조작, LED, 부품 표시
- 헤더에 사용자 원본 BSD 로고 적용. 모바일 390px에서 로고 145px, 이미지 로드 및 가로 넘침 없음 확인.
- 소개 모델 드래그 후 문서 위치 808px 유지, 회전한 각도 유지 확인. 확대 버튼 클릭 후 디스플레이 투영 너비 364px → 425px 증가.
- 모델 위 일반 휠 입력으로 문서 위치 808px → 1448px 이동하고 볼륨 노브 소개 시점으로 복귀 확인.
- 전체 11개 키에 컬러 LED 링과 플레이트 위 빛 번짐 적용. 눌림 시 링과 하위 광원 표현이 함께 숨겨지는 구조 유지.
- 부품의 실제 3D 경계를 투영한 점선 박스와 외부 이름표 적용. 회전·확대 시 함께 이동.
- 최종 점선/연결선 computed color rgb(17,17,17), 노브 이름표 테두리 rgb(212,212,212) 확인.
- 모바일 소개 화면에서도 조작 도구와 부품 이름표가 표시되며 확대 버튼 작동 확인.
- 기능 테스트 20개 통과. 수정한 feature ESLint, TypeScript 및 정적 프로덕션 빌드 통과.

## 최종 후속 조정
- 부품 둘레 박스 제거. 검정 점선이 실제 부품 표면으로 향하고 빨간 화살촉만 1.5초 주기로 은은하게 점멸한다. reduced-motion에서는 정지.
- 컬러 선택 제품도 드래그 회전 가능. 돌린 각도를 유지한 채 White 색상 변경 확인.
- LED는 저채도 파스텔 11색, 얇은 링, 번짐 불투명도 0.25로 완화.
- 색상은 모델이 화면 밖에 있어도 즉시 반영. 히어로의 조명 프리셋/강도를 두 랜딩 모델과 공유.
- 히어로에서 Silver 선택 후 스크롤 소개와 마지막 제품 모두 Silver 실제 화면 확인. 마지막에서 Black 선택 시 세 모델 모두 obsidian 즉시 반영 확인.
- 히어로 컨트롤은 데스크톱 우측 세로 패널로 배치. 모바일에서는 제품 조작 공간을 위해 하단 2열 배치 유지.
- Sound on 기본값 및 히어로 메인 제목 BSD BOT. 확인. 소리는 사용자 입력 시 WebAudio로 재생.
- 사용자 확인 후 Studio 그레이, Daylight 아이보리, After hours 퍼플을 추가해 상품 마감 총 6종으로 확장. 히어로 상단의 기존 조명 모드 탭 제거. 마감 정의를 단일 파일로 공유한다.

## 독립 저장소 검증
- 프로젝트 루트 `/`와 기존 `/grok/` 경로에서 동일한 제품 페이지 제공.
- 새 폴더에서 의존성 설치 후 `npm run check` 전체 통과: 테스트 20개, ESLint, TypeScript 및 정적 빌드.
- 별도 정적 서버에서 화면 렌더링과 콘솔 오류 없음 확인. After hours 선택 시 세 모델 모두 동일 마감 반영.
- 실행 방법과 미연결 출시 알림 서버 범위는 루트 README에 기록.
