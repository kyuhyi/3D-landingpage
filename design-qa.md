# GROK BOT 제품 쇼케이스 디자인 QA

final result: passed

## 비교 대상과 범위
- 원본: `public/images/grok-reference.jpg` (1280 × 715 px).
- 구현: `http://localhost:3100/grok/`.
- 데스크톱: CSS viewport 1280 × 716. CUA 콘텐츠 캡처는 스크롤바/표면 경계를 제외한 1265 × 712 또는 1265 × 708 px. 원본과 두 이미지를 같은 도구 응답에 열어 비교했다. 픽셀 오차 측정 대신 콘텐츠 영역의 형태·계층·비율을 비교했다.
- 모바일: CSS viewport 390 × 844, CUA 콘텐츠 캡처 375 × 812 px. DOM scrollWidth 375로 가로 오버플로 없음.
- 상태: Studio / 조립, 분해 100%, 회전, After hours, 모바일 조립과 분해.
- 사용자 요청은 사진을 활용한 360도 회전·제품 분리 사이트이다. 정지 이미지의 크롭을 그대로 복제하는 대신 전체 제품과 조작부를 표시하는 구성은 의도한 기능상 차이이다. 뒷면과 내부는 콘셉트 모델이며 실측 복제라고 표기하지 않았다.

## 시각 증거
- 전체 비교: `docs/design-references/grok/assembled.png`와 원본을 함께 열어 확인.
- 부품 간격: `docs/design-references/grok/production-exploded.png`.
- 회전: `docs/design-references/grok/rotated.png`.
- 야간 조명: `docs/design-references/grok/night.png`.
- 모바일 초기/수정: `mobile.png`, `mobile-final.png`, `mobile-exploded-final.png` (동일 디렉터리).
- 모델의 키캡·OLED·노브·나사·하판을 전체 비교에서 개별 검토했다. 제품 구조가 모두 보이는 해상도이므로 추가 확대 캡처는 필요하지 않았다. 제조 도면 수준의 치수/표면 검증은 범위 밖이다.

## 발견 사항과 수정 이력
1. [P2, 해결] 최초 모델이 작고 키 아래 광원이 과도하게 밝음. 카메라를 가까이 옮기고 금속/키캡 재질, 광원 세기, 키캡과 조명 띠의 곡률을 수정. `assembled.png`에서 큰 제품과 얇은 가장자리 조명 확인.
2. [P2, 해결] 짧은 데스크톱에서 조작부가 첫 화면 아래로 밀림. 화면 높이별 설명 축약 및 세로 여백 조정. 1280 × 716에서 하단 분해 제어가 보임.
3. [P2, 해결] 100% 분해 시 하판이 뷰어 하단에서 잘림. 분해율에 따라 모델 크기를 최대 18% 줄임. 최종 production-exploded 캡처에서 다섯 레이어 전체 확인.
4. [P2, 해결] 모바일에서 모델 왼쪽이 잘림. 캔버스 높이를 470에서 380으로 조정하여 세로/가로 시야각 비율 개선. mobile-final 및 mobile-exploded-final에서 제품 전체 확인.
5. [P2, 해결] 모바일 분해 버튼 글자가 줄바꿈됨. 버튼 줄바꿈 방지, 슬라이더 폭과 간격 조정. mobile-exploded-final에서 한 줄 제어 확인.

## 필수 디자인 평가
- 타이포그래피: 원본과 같은 절제된 산세리프/모노 조합. 제품 사이트의 큰 영문 제목과 한국어 설명으로 계층 확장. 제목 잘림 없음.
- 간격과 구성: 제품 왼쪽·조명/설명 오른쪽 구성 유지. 데스크톱 조작부 및 모바일 레이아웃 확인.
- 컬러: 흑연색 배경과 금속, 회색 글자, 민트·블루·앰버·라일락 조명. Studio/Daylight/After hours 변경 확인.
- 제품 자산: 사용자 요청에 따라 실제 Three.js 메시로 재구성. 사진을 평면 회전시키는 방식이 아님. 원본 사진은 WebGL 실패 대체 화면에 사용.
- 문구: 제품 콘셉트 표기, 내부 구조 추정 및 실제 AI 연결 미포함을 사양 창에 명확히 설명. 가짜 주문/결제 흐름 없음.

## 기능 검증
- 분해 0→100 및 조립 복귀, 슬라이더 키보드 Home/Right(1%) 확인.
- 분해 상태에서 마우스 드래그로 90도 이상 시점 변화 확인.
- 자동 회전 시작/정지 및 기본 시점 초기화 확인.
- Studio/Daylight/After hours 조명 상태와 화면 변화 확인.
- 실제 제품 키 클릭 및 Research 선택으로 OLED와 활동 문구 변경 확인.
- 사양 모달 열기, Escape 닫기와 트리거 포커스 복원 확인.
- 모바일 390px에서 분해/조립 제어, 문구 줄바꿈, 가로 오버플로 없음 확인.
- 실제 스마트폰 하드웨어의 멀티터치 핀치는 별도 장치에서 검증하지 않음. Pointer/OrbitControls 기반 입력 구현.
- 독립 QA 탭 및 최종 정적 빌드의 콘솔 error 0건. 개발 초기 GPU 셰이더 정밀도 경고는 확인했으나 실제 렌더를 차단하지 않았음.
- 프로덕션 정적 빌드에서도 모델 렌더와 분해 버튼 재확인.

## 코드 검증
- Node 상태 테스트: 4/4 통과.
- 새 기능과 라우트 ESLint: 오류/경고 없음.
- TypeScript: 통과.
- Next.js production build: 통과, `/grok` 정적 경로 생성.
- 기존 `_insane-search` 전체 lint 오류 13개는 작업 전부터 존재하며 이번 기능과 무관.

## 후속 미세 조정
- [P3] 실제 CAD/GLB가 제공되면 추정 모델을 교체하여 키캡 곡면, 소재 텍스처, 내부 구조의 실물 정확도를 높일 수 있다.
- 새 사이트는 기존 메인 교체 승인을 기다리는 동안 `/grok`에 추가했다. 기존 메인 및 전역 스타일은 이 작업에서 수정하지 않았다.
