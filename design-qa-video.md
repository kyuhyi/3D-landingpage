# 영상 기반 GROK BOT 리파인 QA

final result: passed

## 기준
- 사용자 영상: `C:/Users/kyuhy/OneDrive/Documents/카카오톡 받은 파일/KakaoTalk_20260907_124531528.mp4`.
- 1068×596, 24fps, 21.17초. 2초 간격 프레임 및 5.2초/7초/12초/18초 상세 프레임 확인.
- 원본 프레임: `docs/design-references/grok-video/intro.jpg`, `close.jpg`, `angle.jpg`, `exploded.jpg`.
- 구현: `/grok/`, 데스크톱 CSS 1280×716 / 모바일 CSS 390×844.
- CUA의 스크롤바/표면 테두리를 제외한 데스크톱 캡처는 약1265×708, 모바일375×812. 영상과 유사한 화면 비율로 영역/상태를 비교하며 픽셀 단위 동일성을 주장하지 않음.
- 원본 intro/exploded 및 구현 intro/exploded를 동일 도구 응답에 열어 직접 비교.

## 개선한 차이
1. [P1, 해결] 작은 고정 제품+별도 마케팅 영역 → 영상의 전체 화면 스튜디오, 왼쪽 Your team. Within reach. 제목, 오른쪽 조명/활동, 하단 조작부.
2. [P1, 해결] 자동 회전 기본 꺼짐 → 첫 로드부터 36초 루프. 소개, 확대, 상부/측면, 분해, 조립 구간. 드래그 시 즉시 수동 전환.
3. [P1, 해결] 막힌 상판과 단순 사각 스위치 → 실제 메시 개구부, 십자축, 독립 LED 링, 긴 키 스테빌라이저. 기존 사진 버전보다 영상의 분해 구조에 근접.
4. [P2, 해결] 평평한 키캡 → 둥근 곡면과 두께. 밝은 반사를 낮추고 PBT 느낌의 무광 재질 조정.
5. [P2, 해결] OLED 단순 글자 → 봇 얼굴, 모노스페이스 상태 표기, 온라인 표시, 파형 그래픽. 마이크 각인, 컬러 실루엣 아이콘, Orchestrate 표기 반영.
6. [P2, 해결] 조명만 변경 가능 → Obsidian/Porcelain/Copper, 조명 프리셋, 강도 슬라이더, 소리 스위치. 강도에 노브 회전 표시 연동.
7. [P2, 해결] 왼쪽 도구와 설명 겹침 → 도구를 더 왼쪽으로 이동.
8. [P2, 해결] 분해 후 드래그 시 오래된 자동 분해값으로 복귀 → 수동 분해값 보존. 100% 상태에서 드래그 후 슬라이더 100% 유지 확인.
9. [P2, 해결] 모바일 자동 근접 장면의 과도한 확대 → 모바일 카메라 거리 보정. 내부 전체 구조, 하단 제어와 가로 오버플로 없음 확인.

## 시각 증거
- `implementation-intro.png`: 첫 소개 구도. 이후 재질/마이크/Orchestrate 개선 적용.
- `implementation-exploded.png`: 상판 개구부, LED 링, 십자축 확인.
- `implementation-front-exploded-final.png`: 정면에서 5개 레이어 간격 확인. 재질 옵션 테스트 중 캡처이므로 색 비교에는 사용하지 않음.
- `implementation-copper.png`: 구리 마감+주광 및 선택 키 OLED 확인.
- `implementation-mobile.png`: 모바일 분해 투어, 하판까지 프레임 내 표시.
- 제품 영역의 곡면/아이콘/LED 링/개구부를 개별 검토. 영상 압축 및 줌이 들어간 레퍼런스라 정확한 치수나 표면 BRDF는 추정.

## 필수 디자인 평가
- 타이포: 원본의 큰 얇은 산세리프 제목, 작은 모노 라벨을 반영. 제목 및 하단 버튼 겹침 해결.
- 배치: 전체 화면 제품이 중심이며 오른쪽 패널과 하단 바를 레퍼런스 구성으로 재작성.
- 색상: 어두운 바디, 흰 Orchestrate 키, 네 가지 파스텔 키 조명. 세 마감/세 조명의 실제 메시 변경 확인.
- 자산: 사용자 요청에 필요한 실시간 3D 메시. 화면 녹화 비디오를 배경으로 재생하는 대체물이 아님. 내부 구조는 여전히 콘셉트 모델.
- 문구: 레퍼런스의 제목과 조작 라벨을 반영. 디자인 노트에서 콘셉트/미연결 범위 명시.

## 검증
- 자동 진행: INTRO → DETAILS → INSIDE(분해40% 및100%) → 다음 루프의 INTRO/DETAILS 진행을 실제 브라우저에서 확인.
- 수동: 기본/위/정면 시점, 분해100%, 분해 유지 드래그, 키 선택, 마감, 조명, 디자인 노트→구조 보기 확인.
- 소리: Sound on 후 키 입력으로 Web Audio 생성 경로 실행, 콘솔 오류 없음. 실제 스피커 청취는 별도 검증하지 않음.
- 모바일: 390×844, scrollWidth375, 모델 ready, 콘솔 error0.
- Node 테스트11개 통과: 투어3개, 레이어4개, 노브 음량·간격·키 복귀4개.
- 새 기능 ESLint 통과. TypeScript 통과.
- 실제 스마트폰 멀티터치 핀치는 하드웨어에서 별도 검증하지 않음.

## 남는 미세 차이
- [P3] 정확한 CAD가 없어 영상과 키캡 모서리 곡률·노브 가공·빛 반사가 완전히 같지는 않음. 사진 버전의 단순 형상을 크게 개선했지만 실측 복제는 아님.
- 원본 녹화의 컷과 줌을 그대로 재생하지 않고, 사용자가 중간에 조작 가능한 연속 36초 투어로 구성.

## 노브와 키 입력 후속 수정 검증
- 노브 위치를 (2.12, 0.3, -2.24)로 이동. 가장 가까운 키캡과 노브 베이스 사이 여유 0.185 확보.
- 노브 클릭 65→75%, 아래로 50px 드래그 75→45%, 위쪽 휠 45→50% 실제 브라우저 확인. 조명 값과 독립적으로 회전하며 카메라는 유지.
- 하단 음량 슬라이더와 Web Audio 게인 연결. 노브 조작 시 짧은 미리듣기, 0% 무음. 모바일 Home/End로 0/100% 확인.
- Chat 키 pointerdown 유지 중 키가 내려가며 LED 링이 사라짐. pointerup 후 즉시 복귀/LED 재표시. 선택 정보는 OLED에 유지.
- 증거: `docs/design-references/grok-video/input-key-held.png`, `input-key-released.png`.
- 모바일 390×844에서 음량 슬라이더 표시, 가로 넘침 없음. 콘솔 오류 없음.
- 변경 기능 ESLint, TypeScript, 프로덕션 빌드 통과. 기존 루트 페이지/다른 앱 파일은 변경하지 않음.
